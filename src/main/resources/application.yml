spring:
  messages:
    basename: i18n/messages
    encoding: UTF-8
  datasource:
    url: "jdbc:mysql://localhost:3306/minitest"
    username: "root"
    password: "1q2w3e4r%%"
    driver-class-name: "com.mysql.cj.jdbc.Driver"
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  mvc:
    custom-uri-logging-filter:
      enabled: true
      level: info

  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true